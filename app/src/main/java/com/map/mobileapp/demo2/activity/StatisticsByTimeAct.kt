package com.map.mobileapp.demo2.activity

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.map.mobileapp.demo2.R

class StatisticsByTimeAct: AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_statistics_by_time)

        // Implement your logic to display statistics by time
    }
}